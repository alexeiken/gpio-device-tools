[default]
output = mqtt
hostname = test

[sensor1]
type = ds1820
param = 10-00080234149b
id = Temp-1
label = Temperature 1

[sensor2]
type = dummy

[mqtt]
# address of the broker to connect to. Defaults to localhost.
host = localhost
# port of the address broker to connect to. Defaults to 1883.
port = 1883

# MQTT client id
client_id = gio-device-tools
qos = 0
retain = true
keepalive = 60

#username=
#password=
